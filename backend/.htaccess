Options -Indexes

RewriteEngine On

# Allow direct access to real files/directories (e.g. storage assets)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route everything else through index.php
RewriteRule ^ index.php [QSA,L]
